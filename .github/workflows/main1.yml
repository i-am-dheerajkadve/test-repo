name: workflow-github-runner

on:
  push:
    branches:
      - main
jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - name: code checkout
        uses: actions/checkout@v4
        
      - name: testing
        run: echo "This command is also working "

      - name: success
        run: echo " This is success "
